// Copy this file to `lib/secrets.dart` and replace the placeholders with your keys.
// `lib/secrets.dart` is gitignored — do not commit real API keys to the class repository.

const openWeatherApiKey = 'YOUR_OPENWEATHER_API_KEY';

// Google Places API key (enable Places API in Google Cloud).
// Note: if you run Flutter Web, this key will be visible to clients; lock it down with restrictions.
const googlePlacesApiKey = 'YOUR_GOOGLE_PLACES_API_KEY';
